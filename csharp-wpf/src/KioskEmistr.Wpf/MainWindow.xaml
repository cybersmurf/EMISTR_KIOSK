<Window x:Class="KioskEmistr.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="KIOSK_EMISTR"
        Width="1280"
        Height="800"
        Background="#111827"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize"
        KeyDown="Window_KeyDown">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="#1F2937" Padding="12">
      <DockPanel>
        <TextBlock Text="KIOSK_EMISTR" Foreground="White" FontSize="18" FontWeight="SemiBold" />
        <TextBlock Text="WPF runtime shell" Foreground="#CBD5E1" Margin="16,0,0,0" VerticalAlignment="Center" />
      </DockPanel>
    </Border>

    <TabControl x:Name="Tabs" Grid.Row="1" Background="#111827" BorderThickness="0">
      <TabControl.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding Title}" />
        </DataTemplate>
      </TabControl.ItemTemplate>
      <TabControl.ContentTemplate>
        <DataTemplate>
          <ContentPresenter Content="{Binding WebView}" />
        </DataTemplate>
      </TabControl.ContentTemplate>
    </TabControl>
  </Grid>
</Window>
